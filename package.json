{
  "name": "surfaces",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute spotify-agent-db --file=src/db/schema.sql",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.4.0",
    "d3-force": "^3.0.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.15.2",
    "@cloudflare/workers-types": "^4.20250424.0",
    "@types/d3-force": "^3.0.10",
    "@vitest/runner": "^4.1.5",
    "@vitest/snapshot": "^4.1.5",
    "typescript": "^5.8.3",
    "vitest": "^4.1.5",
    "wrangler": "^4.83.0"
  }
}
